<resources>

    <style name="Theme.OpenCoinMap.Splash" parent="Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/Theme.OpenCoinMap</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_inset</item>
    </style>

    <!-- Base application theme. -->
    <style name="Base.Theme.OpenCoinMap" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.OpenCoinMap" parent="Base.Theme.OpenCoinMap" />

</resources>